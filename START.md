# 🚀 Начало работы с Wedding Guest Management

## Самый быстрый способ запустить проект

### Шаг 1: Запустите инициализацию
```bash
./init.sh
```

Этот скрипт автоматически:
- Создаст .env файл
- Соберет Docker образы
- Запустит все контейнеры
- Применит миграции базы данных
- Соберет статические файлы

### Шаг 2: Создайте администратора
```bash
make createsuperuser
```

Или:
```bash
docker-compose exec backend python manage.py createsuperuser
```

### Шаг 3: Откройте приложение
- **Frontend**: http://localhost
- **API**: http://localhost/api/guests/
- **Admin**: http://localhost/admin/

## Альтернативный способ (вручную)

```bash
# 1. Создать .env файл
cp .env.example .env

# 2. Собрать образы
docker-compose build

# 3. Запустить контейнеры
docker-compose up -d

# 4. Применить миграции
docker-compose exec backend python manage.py migrate

# 5. Создать суперпользователя
docker-compose exec backend python manage.py createsuperuser
```

## Полезные команды

```bash
make help          # Показать все доступные команды
make up            # Запустить
make down          # Остановить
make logs          # Показать логи
make restart       # Перезапустить
```

## Документация

- **START.md** ← Вы здесь
- **[README.md](README.md)** - Полная документация проекта
- **[QUICK_START.md](QUICK_START.md)** - Быстрая шпаргалка по командам
- **[DOCKER_GUIDE.md](DOCKER_GUIDE.md)** - Подробное руководство по Docker
- **[PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)** - Итоги проекта
- **[TEST_COMMANDS.md](TEST_COMMANDS.md)** - Команды для тестирования

## Тестирование

```bash
# Создать тестового гостя
curl -X POST http://localhost/api/guests/ \
  -H "Content-Type: application/json" \
  -d '{"first_name":"Тест","last_name":"Тестов"}'

# Получить список гостей
curl http://localhost/api/guests/
```

## Если что-то пошло не так

```bash
# Посмотреть логи
make logs

# Пересоздать контейнеры
make rebuild

# Полная очистка и перезапуск
make clean
make init
```

## Структура проекта

```
wedding_dmitry/
├── backend/          # Django Backend + API
├── frontend/         # React Frontend
├── nginx/            # Nginx Reverse Proxy
├── docker-compose.yml
├── Makefile
└── init.sh
```

## Порты

- **80** - Nginx (главный вход)
- **8000** - Django Backend (напрямую)
- **5173** - React Frontend (напрямую)
- **5432** - PostgreSQL

---

**Готово! Приятной работы! 🎉**

Если нужна помощь, читайте подробную документацию в README.md
