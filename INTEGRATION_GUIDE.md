# 🔗 Руководство по интеграции Frontend и Backend

## ✅ Что было реализовано

### Frontend (GuestForm.jsx)
- ✅ Отправка POST запроса на backend при нажатии кнопки "Подтвердить"
- ✅ Индикатор загрузки во время отправки данных
- ✅ Блокировка формы во время отправки (disabled состояние)
- ✅ Отображение сообщений об успехе и ошибках
- ✅ Автоматическая очистка формы после успешной отправки
- ✅ Параллельная отправка нескольких гостей (Promise.all)
- ✅ Обработка ошибок с информативными сообщениями

### Backend API
- ✅ REST API endpoint: `POST /api/guests/`
- ✅ Валидация данных (first_name, last_name)
- ✅ Сохранение в PostgreSQL
- ✅ CORS настроен для frontend

### Docker Integration
- ✅ Nginx проксирует запросы с frontend на backend
- ✅ Переменные окружения для API URL
- ✅ Все сервисы в одной Docker сети

## 🔌 API Endpoint

### Создание гостя
```http
POST /api/guests/
Content-Type: application/json

{
  "first_name": "Иван",
  "last_name": "Иванов"
}
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "first_name": "Иван",
  "last_name": "Иванов"
}
```

## 🧪 Тестирование

### 1. Открыть приложение
```bash
open http://localhost:8080
```

### 2. Заполнить форму
- Введите имя и фамилию гостя
- Нажмите "Добавить гостя" если хотите добавить больше гостей (до 4)
- Нажмите кнопку "Подтвердить"

### 3. Проверить результат
При успешной отправке вы увидите:
```
🎉 Спасибо! Вы успешно подтвердили участие для:

Иванов Иван

Мы с нетерпением ждём вас на нашей свадьбе!
```

### 4. Проверить данные в backend
```bash
# Через API
curl http://localhost:8080/api/guests/

# Через Django Admin
open http://localhost:8080/admin/

# Прямое подключение к БД
docker-compose exec db psql -U wedding_user -d wedding_db -c "SELECT * FROM guests_guest;"
```

## 🎨 UI/UX Features

### Состояния формы

**1. Обычное состояние:**
- Активные поля ввода
- Кнопка "Подтвердить" активна

**2. Загрузка (loading):**
- Все поля заблокированы (disabled)
- Кнопка показывает "Отправка..."
- Opacity 0.5 для disabled элементов

**3. Успех (success):**
- Зелёное сообщение с эмодзи 🎉
- Анимация появления (slideDown)
- Автоочистка через 5 секунд

**4. Ошибка (error):**
- Красное сообщение с эмодзи ❌
- Детальное описание ошибки
- Форма остаётся заполненной для повтора

### CSS Классы

```css
.submit-status.success  /* Зелёное сообщение об успехе */
.submit-status.error    /* Красное сообщение об ошибке */
button:disabled         /* Заблокированное состояние */
input:disabled          /* Заблокированный ввод */
```

## 🔧 Конфигурация

### Переменные окружения

**`.env` файл:**
```env
VITE_API_URL=http://localhost:8080/api
```

**Или в `docker-compose.yml`:**
```yaml
frontend:
  environment:
    - VITE_API_URL=http://localhost:8080/api
```

### API URL Priority

1. Переменная окружения: `import.meta.env.VITE_API_URL`
2. Fallback: `http://localhost:8080/api`

## 📊 Поток данных

```
┌─────────────┐
│   Browser   │
│  (Frontend) │
└──────┬──────┘
       │ POST /api/guests/
       ▼
┌─────────────┐
│    Nginx    │
│  (Port 80)  │
└──────┬──────┘
       │ proxy_pass
       ▼
┌─────────────┐
│   Django    │
│  (Backend)  │
└──────┬──────┘
       │ Save
       ▼
┌─────────────┐
│ PostgreSQL  │
│  (Database) │
└─────────────┘
```

## 🚀 Развертывание

### Development
```bash
# Запустить все сервисы
docker-compose up -d

# Пересобрать frontend после изменений
docker-compose build frontend
docker-compose up -d frontend
```

### Production
Измените API URL на production домен:
```env
VITE_API_URL=https://your-domain.com/api
```

## 🐛 Troubleshooting

### Frontend не может подключиться к API

**Проверьте CORS:**
```bash
docker-compose logs backend | grep CORS
```

**Проверьте API URL:**
```bash
docker-compose exec frontend sh
echo $VITE_API_URL
```

### Ошибка "Network request failed"

**1. Проверьте, что все сервисы запущены:**
```bash
docker-compose ps
```

**2. Проверьте доступность API:**
```bash
curl http://localhost:8080/api/guests/
```

**3. Проверьте логи nginx:**
```bash
docker-compose logs nginx
```

### Форма не отправляется

**1. Откройте Developer Console (F12)**
- Проверьте Network tab
- Посмотрите на ошибки в Console

**2. Проверьте формат данных:**
```javascript
// Правильно
{
  "first_name": "Иван",
  "last_name": "Иванов"
}

// Неправильно
{
  "firstName": "Иван",  // ❌ должно быть first_name
  "lastName": "Иванов"  // ❌ должно быть last_name
}
```

## 📝 Примеры использования

### Одиночный гость
```javascript
// Пользователь вводит:
Фамилия: Иванов
Имя: Иван

// Отправляется:
POST /api/guests/
{
  "first_name": "Иван",
  "last_name": "Иванов"
}
```

### Несколько гостей
```javascript
// Пользователь вводит:
1. Иванов Иван
2. Петрова Мария
3. Сидоров Алексей

// Отправляется 3 параллельных запроса:
Promise.all([
  POST /api/guests/ { "first_name": "Иван", "last_name": "Иванов" },
  POST /api/guests/ { "first_name": "Мария", "last_name": "Петрова" },
  POST /api/guests/ { "first_name": "Алексей", "last_name": "Сидоров" }
])
```

## 🎯 Следующие шаги

Возможные улучшения:
- [ ] Добавить email поле для гостей
- [ ] Добавить телефон для связи
- [ ] Добавить выбор меню (взрослое/детское)
- [ ] Добавить комментарии/пожелания
- [ ] Добавить подтверждение по email
- [ ] Добавить проверку дубликатов
- [ ] Добавить редактирование уже добавленных гостей
- [ ] Добавить статистику по гостям

## 📚 Дополнительная информация

- [Backend API Documentation](./backend/API_DOCS.md)
- [Frontend Components](./frontend/COMPONENTS.md)
- [Docker Guide](./DOCKER_GUIDE.md)

---

**Готово! Интеграция работает на 100%! 🎉**

