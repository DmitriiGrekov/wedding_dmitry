# Быстрый старт

## 🚀 Запуск React приложения

### 1. Установка зависимостей (первый раз)
```bash
npm install
```

### 2. Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

### 3. Сборка для продакшена
```bash
npm run build
```

Готовые файлы будут в папке `dist/`

## 📝 Быстрая настройка

### Изменить дату свадьбы
Файл: `src/App.jsx`
```javascript
const eventDate = new Date('2026-07-18T15:00:00+03:00');
```

### Изменить имена
Файлы: 
- `src/components/EnvelopeAnimation.jsx` (в конверте)
- `src/components/MainSection.jsx` (главная секция)

### Изменить ссылку на Telegram
Файл: `src/components/EventInfo.jsx`
```jsx
<TelegramQR telegramLink="https://t.me/ваша_ссылка" />
```

### Изменить карту
Файл: `src/components/Schedule.jsx`
Замените `src` в iframe на вашу карту с Яндекс.Карт

### Изменить фоновые изображения
Замените файлы в папке `public/`:
- `background-header.png` - фон для главной секции
- `background.jpg` - фон для секции с информацией

## 📱 Проверка на мобильных устройствах

В Chrome/Firefox откройте инструменты разработчика (F12) и включите режим мобильного устройства для проверки адаптивности.

## 🎨 Стили

Все стили находятся рядом с компонентами:
- `src/components/ComponentName.css` - стили для конкретного компонента
- `src/App.css` - глобальные стили приложения
- `src/index.css` - базовые стили и подключение шрифтов

## 🐛 Решение проблем

### Приложение не запускается
```bash
# Удалите node_modules и установите заново
rm -rf node_modules package-lock.json
npm install
```

### Ошибка портов
Если порт 3000 занят, измените в `vite.config.js`:
```javascript
server: {
  port: 3001  // или другой свободный порт
}
```

## 📦 Структура проекта

```
wedding_dmitry/
├── public/              # Статические файлы (изображения)
├── src/
│   ├── components/      # React компоненты
│   ├── App.jsx          # Главный компонент
│   ├── main.jsx         # Точка входа
│   └── index.css        # Глобальные стили
├── исходники/           # Оригинальные HTML файлы
├── index.html           # HTML для React
└── package.json         # Зависимости проекта
```

## ✅ Готово к продакшену

После сборки (`npm run build`) загрузите содержимое папки `dist/` на ваш хостинг.

**Рекомендуемые хостинги:**
- Vercel (бесплатно)
- Netlify (бесплатно)
- GitHub Pages
- Обычный хостинг (просто загрузите файлы)

