# 🚀 Инструкция по развертыванию на production сервере с HTTPS

Эта инструкция описывает процесс развертывания свадебного сайта на production сервере с автоматическим получением и продлением SSL сертификатов от Let's Encrypt.

## 📋 Требования

- VPS/сервер с Ubuntu 20.04+ (или другой Linux)
- Docker и Docker Compose установлены
- Зарегистрированный домен с настроенными DNS записями
- Root или sudo доступ к серверу

## 🔧 Подготовка сервера

### 1. Установка Docker и Docker Compose

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавляем текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Устанавливаем Docker Compose
sudo apt install docker-compose-plugin -y

# Проверяем установку
docker --version
docker compose version
```

### 2. Настройка DNS

Убедитесь, что DNS записи вашего домена указывают на IP адрес сервера:

```
A     @           YOUR_SERVER_IP
A     www         YOUR_SERVER_IP
```

Проверить можно командой:
```bash
dig your-domain.com
dig www.your-domain.com
```

### 3. Настройка Firewall

```bash
# Разрешаем HTTP, HTTPS и SSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp

# Включаем firewall
sudo ufw enable

# Проверяем статус
sudo ufw status
```

## 📦 Развертывание приложения

### 1. Клонирование репозитория

```bash
# Клонируем проект на сервер
git clone <your-repository-url> wedding
cd wedding
```

### 2. Настройка переменных окружения

```bash
# Создаем файл с переменными окружения из примера
cp env.prod.example .env.prod

# Редактируем файл
nano .env.prod
```

Заполните следующие переменные:

```bash
# Ваш домен (БЕЗ https://)
DOMAIN=your-domain.com

# Email для уведомлений Let's Encrypt
EMAIL=your-email@example.com

# Секретный ключ Django (сгенерируйте новый!)
SECRET_KEY=your-super-secret-key-here

# DEBUG должен быть False на production
DEBUG=False

# Настройки базы данных
POSTGRES_DB=wedding_db
POSTGRES_USER=wedding_user
POSTGRES_PASSWORD=strong-password-here
```

**Генерация SECRET_KEY:**
```bash
python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### 3. Получение SSL сертификатов

Скрипт автоматически получит SSL сертификаты от Let's Encrypt:

```bash
# Делаем скрипт исполняемым
chmod +x init-letsencrypt.sh

# Запускаем скрипт инициализации SSL
./init-letsencrypt.sh
```

Скрипт выполнит следующие действия:
1. Проверит наличие необходимых переменных окружения
2. Создаст временную nginx конфигурацию
3. Получит SSL сертификаты от Let's Encrypt
4. Настроит финальную конфигурацию с HTTPS
5. Запустит все сервисы

### 4. Ручной запуск (если не используете скрипт)

Если хотите запустить вручную:

```bash
# Собираем образы
docker compose -f docker-compose.prod.yml build

# Запускаем сервисы
docker compose -f docker-compose.prod.yml up -d

# Проверяем статус
docker compose -f docker-compose.prod.yml ps
```

### 5. Создание суперпользователя Django

```bash
# Входим в контейнер backend
docker compose -f docker-compose.prod.yml exec backend sh

# Создаем суперпользователя
python manage.py createsuperuser

# Выходим
exit
```

## 🔄 Автоматическое продление сертификатов

SSL сертификаты будут автоматически продлеваться каждые 12 часов благодаря контейнеру certbot. Nginx перезагружается каждые 6 часов для применения обновленных сертификатов.

## 🎯 Проверка работоспособности

### Проверка сайта

1. Откройте браузер и перейдите на `https://your-domain.com`
2. Проверьте, что сайт открывается по HTTPS
3. Убедитесь, что SSL сертификат валидный (зеленый замок в браузере)

### Проверка API

```bash
curl https://your-domain.com/api/
```

### Проверка admin панели

Откройте `https://your-domain.com/admin/` и войдите с созданными учетными данными.

### Просмотр логов

```bash
# Все сервисы
docker compose -f docker-compose.prod.yml logs -f

# Только backend
docker compose -f docker-compose.prod.yml logs -f backend

# Только nginx
docker compose -f docker-compose.prod.yml logs -f nginx

# Только certbot
docker compose -f docker-compose.prod.yml logs -f certbot
```

## 🔧 Обслуживание

### Обновление приложения

```bash
# Останавливаем сервисы
docker compose -f docker-compose.prod.yml down

# Получаем последние изменения
git pull origin main

# Пересобираем образы
docker compose -f docker-compose.prod.yml build

# Запускаем обновленные сервисы
docker compose -f docker-compose.prod.yml up -d
```

### Резервное копирование базы данных

```bash
# Создаем backup
docker compose -f docker-compose.prod.yml exec db pg_dump -U wedding_user wedding_db > backup_$(date +%Y%m%d).sql

# Восстановление из backup
docker compose -f docker-compose.prod.yml exec -T db psql -U wedding_user wedding_db < backup_20240101.sql
```

### Просмотр использования ресурсов

```bash
# Статистика контейнеров
docker stats

# Использование дискового пространства
docker system df
```

### Очистка неиспользуемых ресурсов

```bash
# Удаляем неиспользуемые образы и контейнеры
docker system prune -a

# Удаляем неиспользуемые volumes (ОСТОРОЖНО!)
docker volume prune
```

## 🚨 Устранение неполадок

### SSL сертификат не получается

1. Проверьте, что DNS записи указывают на ваш сервер
2. Убедитесь, что порты 80 и 443 открыты
3. Проверьте логи certbot: `docker compose -f docker-compose.prod.yml logs certbot`

### Сайт не открывается

1. Проверьте статус сервисов: `docker compose -f docker-compose.prod.yml ps`
2. Проверьте логи: `docker compose -f docker-compose.prod.yml logs`
3. Убедитесь, что firewall настроен правильно: `sudo ufw status`

### База данных не подключается

1. Проверьте переменные окружения в `.env.prod`
2. Проверьте логи базы данных: `docker compose -f docker-compose.prod.yml logs db`
3. Проверьте health check: `docker compose -f docker-compose.prod.yml exec db pg_isready`

## 📊 Мониторинг

Рекомендуется настроить мониторинг для production:

- **Uptime monitoring**: UptimeRobot, Pingdom
- **Application monitoring**: Sentry для отслеживания ошибок
- **Server monitoring**: Netdata, Prometheus + Grafana

## 🔒 Безопасность

1. Регулярно обновляйте систему: `sudo apt update && sudo apt upgrade`
2. Используйте сложные пароли для базы данных
3. Никогда не коммитьте `.env.prod` в git
4. Регулярно делайте резервные копии
5. Настройте автоматические обновления безопасности

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи контейнеров
2. Убедитесь, что все переменные окружения заданы правильно
3. Проверьте документацию Docker и Let's Encrypt
4. Создайте issue в репозитории проекта

## ✅ Checklist развертывания

- [ ] Docker и Docker Compose установлены
- [ ] DNS записи настроены и работают
- [ ] Firewall настроен (порты 80, 443, 22)
- [ ] Файл `.env.prod` создан и заполнен
- [ ] SSL сертификаты получены
- [ ] Сервисы запущены и работают
- [ ] Суперпользователь Django создан
- [ ] Сайт доступен по HTTPS
- [ ] Настроено резервное копирование
- [ ] Настроен мониторинг

---

🎉 **Поздравляем! Ваш свадебный сайт развернут на production с HTTPS!**

