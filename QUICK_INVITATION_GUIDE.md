# 🎉 Быстрый старт: Персонализированные приглашения

## Что было сделано?

✅ Создана модель `Invitation` для хранения персонализированных приглашений  
✅ Добавлен API endpoint для получения приглашения по UUID  
✅ Обновлен фронтенд для автоматической загрузки и отображения имен гостей  

## Быстрый старт (5 минут)

### 1. Применить миграции (2 мин)

```bash
cd backend
python manage.py makemigrations
python manage.py migrate
```

### 2. Запустить сервер (1 мин)

```bash
# Терминал 1 - Backend
cd backend
python manage.py runserver

# Терминал 2 - Frontend
cd frontend
npm run dev
```

### 3. Создать приглашение (2 мин)

Откройте админ-панель: http://localhost:8000/admin/

**Вариант A: Для одного гостя**
- Приглашения → Добавить
- Имена гостей: `Иван Петров`
- Множественное число: ☐ (не отмечено)
- Сохранить

**Вариант B: Для пары/семьи**
- Имена гостей: `Иван и Мария Петровы`
- Множественное число: ☑ (отмечено)
- Сохранить

### 4. Получить ссылку

После сохранения скопируйте UUID из админ-панели.

Ваша персонализированная ссылка:
```
http://localhost:5173/?uuid=СКОПИРОВАННЫЙ_UUID
```

## Примеры результатов

| Имена гостей | Множественное | Результат на сайте |
|--------------|--------------|-------------------|
| Иван Петров | ☐ Нет | **Дорогой** Иван Петров! |
| Иван и Мария Петровы | ☑ Да | **Дорогие** Иван и Мария Петровы! |
| Семья Петровых | ☑ Да | **Дорогие** Семья Петровых! |

## Создание приглашений через Shell (опционально)

```bash
cd backend
python manage.py shell
```

```python
from guests.models import Invitation

# Создать приглашение
inv = Invitation.objects.create(
    guest_names="Алексей Смирнов",
    is_plural=False
)

print(f"Ссылка: http://localhost:5173/?uuid={inv.uuid}")
```

## Как отправить гостям?

1. Создайте приглашение в админ-панели
2. Скопируйте UUID
3. Отправьте гостю ссылку:
   ```
   http://yourdomain.com/?uuid=ВСТАВЬТЕ_UUID
   ```

## Проверка работы

1. Откройте созданную ссылку в браузере
2. На странице должно отобразиться:
   - Персонализированное приветствие
   - "Дорогой/Дорогие" в зависимости от флага
   - Имена гостей

## Что если UUID не указан?

Если открыть сайт без UUID (`http://localhost:5173/`), отобразится стандартное приветствие:
```
Дорогой
Гость!
```

## Полезные ссылки

- [Подробная инструкция](INVITATION_SETUP.md)
- [API документация](backend/API_INVITATION.md)
- Django Admin: http://localhost:8000/admin/
- Фронтенд: http://localhost:5173/

## Структура базы данных

```sql
-- Таблица guests_invitation
uuid          | UUID (уникальный, автогенерация)
guest_names   | TEXT (имена для отображения)
is_plural     | BOOLEAN (множественное число)
created_at    | DATETIME (дата создания)
```

## Частые вопросы

**Q: Как создать много приглашений быстро?**
A: Используйте Django shell или создайте CSV файл и импортируйте через admin action.

**Q: Можно ли изменить приглашение после создания?**
A: Да, в админ-панели можно редактировать имена и флаг множественного числа. UUID останется прежним.

**Q: Что делать, если гость потерял ссылку?**
A: Найдите его приглашение в админ-панели по именам и отправьте ссылку повторно.

## Troubleshooting

**Приглашение не загружается на фронтенде:**
- Проверьте, что бэкенд запущен (http://localhost:8000)
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что UUID скопирован полностью

**404 ошибка в консоли:**
- Проверьте, что UUID существует в базе данных
- Проверьте правильность URL: `/api/guests/invitations/by-uuid/{uuid}/`

**CORS ошибка:**
- Убедитесь, что в `backend/backend/settings.py` добавлен ваш фронтенд домен в `CORS_ALLOWED_ORIGINS`

